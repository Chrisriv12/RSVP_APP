<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RSVP_APP.MainPage"
             Title="Welcome">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">

            <Label Text="Welcome to RSVP App"
                   FontSize="28"
                   HorizontalOptions="Center"
                   TextColor="#2B2B2B"
                   FontAttributes="Bold" />

            <Label Text="Create and manage events with ease."
                   FontSize="16"
                   HorizontalOptions="Center"
                   TextColor="Gray"
                   Margin="0,0,0,20" />

            <Button Text="Login"
                    Clicked="OnLoginClicked"
                    BackgroundColor="#0078D7"
                    TextColor="White"
                    CornerRadius="10"
                    HeightRequest="50" />

            <Button Text="Continue as Guest"
                    Clicked="OnGuestClicked"
                    BackgroundColor="#AAAAAA"
                    TextColor="White"
                    CornerRadius="10"
                    HeightRequest="50" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
